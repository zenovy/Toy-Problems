function scheduler (meetingTuples) {
  var startPoints = [];
  var endPoints = [];
  for (var i = 0; i < meetingTuples.length; i++) {
    var start = meetingTuples[i][0];
    var end = meetingTuples[i][1];
  }

  
}
